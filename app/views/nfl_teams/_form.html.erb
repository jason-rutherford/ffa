<%= form_for(nfl_team) do |f| %>
  <% if nfl_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nfl_team.errors.count, "error") %> prohibited this nfl_team from being saved:</h2>

      <ul>
      <% nfl_team.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %>
    <%= f.text_field :code %>
  </div>

  <div class="field">
    <%= f.label :full_name %>
    <%= f.text_field :full_name %>
  </div>

  <div class="field">
    <%= f.label :short_name %>
    <%= f.text_field :short_name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
